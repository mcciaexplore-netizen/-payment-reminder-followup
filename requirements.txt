# Direct dependencies pinned to the versions used for development and checks.
# Keep pins aligned with pyproject.toml, which Vercel uses for installation.
# AI wording is optional; provider quotas and costs depend on the account.
groq==1.7.0
streamlit==1.62.0
pandas==3.0.3
openpyxl==3.1.5
python-dotenv==1.2.2
tzdata==2026.3
cryptography==50.0.1
httpx==0.28.1
starlette==1.6.0
uvicorn==0.53.0
libsql==0.1.11
sqlitecloud==0.0.84
